# Copy to .env.local (gitignored) for local dev, or set these in Vercel project env.
# Leaving these unset runs the app entirely on local mock data (mock-first).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
